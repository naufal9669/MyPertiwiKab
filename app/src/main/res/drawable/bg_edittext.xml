<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna dasar putih -->
    <solid android:color="#FFFFFF" />

    <!-- Sudut melengkung -->
    <corners android:radius="20dp" />

    <!-- Sedikit bayangan halus -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="10dp"
        android:bottom="8dp" />

    <!-- Garis batas tipis agar tampak elegan -->
    <stroke
        android:width="1dp"
        android:color="#DADADA" />
</shape>
