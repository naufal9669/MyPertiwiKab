<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_tk_pertiwi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_tk_pertiwi"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPertiwiKab">
        <activity
            android:name=".PengajuanIzinActivity"
            android:exported="false" />
        <!-- Login: halaman pertama saat aplikasi dibuka -->
        <activity android:name=".AbsensiDatangActivity" />
        <activity android:name=".AbsensiPulangActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPertiwiKab.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Register -->
        <activity android:name=".RegisterActivity" /> <!-- Dashboard (halaman utama setelah login) -->
        <activity android:name=".FiturActivity"
            android:theme="@style/Theme.MyPertiwiKab.NoActionBar"
            />


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>